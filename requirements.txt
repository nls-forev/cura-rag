# Hugging Face Spaces installs this before copying the repo, so it cannot be
# "pip install .". List runtime deps explicitly instead. Keep in sync with the
# [project].dependencies in pyproject.toml. torch and gradio are omitted on
# purpose: the Space base image pins both (ZeroGPU).
pydantic>=2.6
pydantic-settings>=2.2
qdrant-client>=1.9
rank-bm25>=0.2.2
sentence-transformers>=2.7
langchain-huggingface>=0.0.3
langchain-core>=0.2
langchain-text-splitters>=0.2
numpy>=1.26
httpx>=0.27
pypdf>=4.2
openai>=1.30
